apiVersion: v1
kind: Service
metadata:
  name: task-service
  # Internal service - use ClusterIP for internal routing
spec:
  selector:
    app: task-service
  ports:
    - name: http
      port: 80
      targetPort: 5001
  type: ClusterIP
