apiVersion: v1
kind: Service
metadata:
  name: board-service
  # Internal service - use ClusterIP so it's not exposed externally
spec:
  selector:
    app: board-service
  ports:
    - name: http
      port: 80
      targetPort: 8002
  type: ClusterIP
