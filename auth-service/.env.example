PORT=8000
# Mongo connection for local dev (change to your DB URI or use k8s Secret)
MONGO_URI=mongodb://mongo:27017/smart_task_manager
# Replace with a strong secret. Rotate if leaked.
JWT_SECRET=REPLACE_ME_WITH_A_STRONG_SECRET
